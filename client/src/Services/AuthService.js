const AuthService = {
    login: (username, password) => {
      // Handle login
    },
    logout: () => {
      // Handle logout
    },
    isAuthenticated: () => {
      // Check if the user is authenticated
      return true;
    }
  };
  
  export default AuthService;
  